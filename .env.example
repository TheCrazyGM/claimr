# Example .env file for RC Cost Chart
# Copy this file to .env and fill in your actual values

# Path to the RC cost database (default: sqlite:///rc_costs.db)
RC_COST_DB_PATH=sqlite:///rc_costs.db

# (Optional) Flask secret key for sessions
# SECRET_KEY=your_secret_key_here

# (Optional) Flask environment (development/production)
# FLASK_ENV=development
